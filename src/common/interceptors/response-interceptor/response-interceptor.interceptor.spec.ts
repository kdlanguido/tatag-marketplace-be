import { ResponseInterceptorInterceptor } from './response-interceptor.interceptor';

describe('ResponseInterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new ResponseInterceptorInterceptor()).toBeDefined();
  });
});
